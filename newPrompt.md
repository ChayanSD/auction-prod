create a page where we will fetch all auctions which data  is under a category
so you have fetcbh this from @app/api/auction/route.ts 
for data fetching use rtk query , and use proper typescript , do not use type any

so we have planned like this
there is category -> auctions  -> auctions items
so it is like designed grandparent -> parent -> child

so there is already a category page and single product 
now we need to mak